# Database
spring.datasource.url=jdbc:mysql://localhost:3306/db_1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (dialect auto-detected for MySQL 8+; remove explicit setting)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/db_1?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=user
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (dialect auto-detected for MySQL 8+; remove explicit setting)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT config
app.jwt.secret=change-this-to-a-long-secret-key-123456789
app.jwt.issuer=web-ai
app.jwt.expiration=12000

# Face Recognition API
face.recognition.api-url=https://untimed-kasen-invigoratingly.ngrok-free.dev
face.recognition.api-timeout=30000
face.recognition.similarity.threshold=0.35
face.recognition.enabled=true